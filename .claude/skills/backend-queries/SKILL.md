---
name: Backend Queries
description: Write secure, performant database queries using parameterized queries, proper indexing, and N+1 prevention strategies. Use this skill when writing database queries, implementing data fetching logic, working with ORMs, or modifying files in /repositories/, /queries/, /services/, or any file containing database operations. Apply when implementing eager loading, writing raw SQL, optimizing query performance, or adding query caching.
---

## When to use this skill:

- When writing database queries (SQL or ORM methods)
- When working on files in `/repositories/`, `/queries/`, `/services/`, or `/lib/db/`
- When implementing data fetching logic in server-side code
- When preventing N+1 query problems with eager loading
- When optimizing slow queries or adding indexes
- When implementing query caching strategies
- When writing raw SQL queries
- When using Prisma queries, Sequelize finders, or similar ORM methods
- When implementing database transactions
- When adding query timeouts or pagination

# Backend Queries

This Skill provides Claude Code with specific guidance on how to adhere to coding standards as they relate to how it should handle backend queries.

## Instructions

For details, refer to the information provided in this file:
[backend queries](../../../agent-os/standards/backend/queries.md)
